import React from 'react';
import ReactDOM from 'react-dom';
import RoutedApp from './routes/index';

ReactDOM.render(
  <React.StrictMode>
    <RoutedApp />
  </React.StrictMode>,
  document.getElementById('root')
);
